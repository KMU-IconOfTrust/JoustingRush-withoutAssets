using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EffectiveSoundController : MonoBehaviour
{
    // 효과음 컨트롤러
    // Player / Enemy의 Rider Object 에 붙어서 작동

    // AudioSource는 각 캐릭터가 지니고 있음

    // 각 효과음 클립들
    public AudioClip weaponSound;
    public AudioClip shieldSound;
    public AudioClip riderSound;

    // Update is called once per frame
    void Update()
    {
        
    }
}
